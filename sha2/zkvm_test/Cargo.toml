[package]
name = "zkvm-host-test"
version = "0.1.0"
edition = "2018"

[dependencies]
openvm-sdk = { git = "https://github.com/openvm-org/openvm", branch = "develop-v2.0.0-rc.2", default-features = false }
openvm-sdk-config = { git = "https://github.com/openvm-org/openvm", branch = "develop-v2.0.0-rc.2" }
openvm-build = { git = "https://github.com/openvm-org/openvm", branch = "develop-v2.0.0-rc.2" }
openvm-stark-sdk = { git = "https://github.com/openvm-org/stark-backend.git", branch = "develop-v2", default-features = false, features = ["cpu-backend"] }
eyre = "0.6"

[[bin]]
name = "verify-sha2-chips"
path = "src/main.rs"

# Keep this package independent from the parent workspace
[workspace]
